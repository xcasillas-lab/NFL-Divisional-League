DFFL STEP 1 — STANDINGS ONLY

Upload BOTH files to the root of your GitHub repository:

1. index.html
   - Same working single-page site.
   - Standings content has been removed from index.html.
   - Standings tab now opens standings.html.
   - Other tabs remain inside index.html.
   - Hash navigation is enabled so URLs such as #newsletter and #rules work.

2. standings.html
   - Standalone Standings page.
   - Uses the same DFFL header, navigation, colors, and styling.
   - Other navigation buttons return to the correct tab inside index.html.

DO NOT upload any css/js/assets folders for this step.
DO NOT change CNAME.
